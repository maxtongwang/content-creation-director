---
name: content-creation-director
description: 中文自媒体编导工作流。说「编导」「跑编导」，或提到任一环节名（0-intake / 1-account / 1b / 1c / 1d-scan / 2-ip / 3-topic / 4-hook / 5-script / 6-edit / 6b-autoedit / 7-review / 8-styles / 9-deliver / 10-training / 11-audit），或在讨论做视频、做内容、做账号时，直接进入本 skill。覆盖账号定位、人设 IP、选题、标题钩子、脚本分镜、剪辑交付、复盘诊断、编导训练，产出结构化文件在环节间传递，末端可驱动自动剪辑。用户提到起号、爆款、标题怎么写、开头怎么抓人、写脚本、怎么剪、数据不好、复盘、内容没方向、小红书 YouTube 运营、跨平台分发，或说「这条选题行不行」「帮我看看我的号」「扫一下我的号」时务必使用。即使只问一个很小的问题（某个标题好不好），也应使用——那几乎都是定位问题的表层症状。连了 MCP 时可直接读账号与素材库真实数据。 English: triggers on "编导", "director workflow", any stage name above, or any discussion of making videos/content — Xiaohongshu/RedNote/YouTube strategy, positioning, topic ideas, titles and hooks, scripts, b-roll editing, why views dropped, account review.
---

# 编导工作流

统一入口：**理解需求 · 选择路径 · 整合验收**。

这不是一条流水线，是一张**网**。用户可以从任何一点进入，环节之间可以横向跳转，
问题可以回流到任意上游。唯一固定的是出口——所有产出都过 `9-deliver` 验收。

---

## 开工第一句 ★ 让用户看得见，且可验证

进入本 skill 后，**第一句必须是**：

```
[编导] 走 <环节名> · 已读 <实际 view 过的文件列表>
```

例：

```
[编导] 走 3-topic · 已读 workflows/3-topic.md, references/platform-playbook.md
```

### 硬规则

1. **声明前必须先 view 那些文件。**工具调用是唯一凭证——
   没有 view 记录就是没读，**不允许凭记忆写文件名**。
2. **参与写过这个 skill 不等于读过。**长对话中最常见的失效模式是
   「我知道里面写了什么」——那是记忆，不是执行。每次开工重新读。
3. **跨环节跳转要重新声明。**从 `3-topic` 跳到 `4-hook`，重新报一次。
4. **用户可随时喊「跑 X」强制指定环节**，此时同样先 view 再声明。

### 为什么必须这样

skill 是否被触发、流程是否被执行，用户从外面看不出来。
没有这行标记和对应的 view 调用，一份泛泛的建议和一次真正走流程的产出长得一样。

**用户看不到标记，就说明 skill 没生效——那是他需要知道的事实。**

---

## 三十三条核心原则

**一、定位是闸门，爆款是放大。**
默认顺序：定位 → 选题（合定位 + 合热点）→ 爆款增幅。
只有用户明确说「只要爆款不要定位」时才跳过闸门。

**二、一条数据流贯穿始终。**

```
profile.yaml  →  topic-card  →  script  →  edit-plan  →  ledger  →  diagnose ↺
   我是谁         拍什么        怎么拍      怎么剪       发了什么     哪出问题
```

上一环节的输出是下一环节的**输入**，不是参考。

**三、剪辑之所以能像成片，是因为分镜在脚本阶段就定完了。**
剪出来不对，先回脚本查 `cut_reason`，不要在剪辑参数上打转。

**四、风格是一组剪辑参数，不是标签。**
一个博主可以有多种风格，剪辑节奏完全不同。剪之前先识别，识别不出就问。

**五、逼出否定比收集信息更重要。**
一份没有 `forbidden_topics` 的 profile 是废的。

**六、能循环的地方一定要循环。**
验收不过、复盘发现问题、能力有短板——都回到对应环节重做，不要将就往下走。

**七、有什么用什么，缺什么补什么。**
流程是网不是表单。**不要求用户先填齐再开工。**
先榨干他已经说的，再看能不能自动读，再合理推断（标 `inferred`），
剩下的才问——而且只问「缺了这一步就做不下去」的那几个。
判断标准：**一个真人编导在这个信息量下会怎么做，就怎么做。**

**八、默认生成，不默认评判。**
用户来了通常是**要东西**，不是要打分。
「这个标题怎么样」的正确回应不是打个分，是**再给十个更好的**，然后说明为什么。
闸门和验收是**筛选器，不是入口**——先生成一批，再用闸门滤，
而不是等用户拿东西来给你审。

**默认产出量**（除非用户明说只要一个）：

| 要什么 | 至少给 |
|---|---|
| 选题 | 8 个，跨不同角度与爆款类型 |
| 标题 | 10 个，覆盖全部句式 |
| 开头第一句 | 5 句，不同句式 |
| 封面思路 | 3 个 |
| 系列名 | 5 个 |
| 账号名 / 简介 | 各 5 个 |

**给完再排序**，标出推荐哪个、为什么。**挑选权留给人。**

**九、数据决定做什么，本人决定怎么做人。**

```
数据决定：拍什么、什么形态、什么配比
本人决定：用什么态度、什么边界、什么语气
```

数据能验证「做什么有效」，验证不了「该怎么做人」。**两层都要，不是替代关系。**

| 层 | 字段 | 谁说了算 |
|---|---|---|
| 数据层 | `pillars` `content_lines` `targets` 权重 `series` `styles` | ledger 数据，随表现修正 |
| 价值观层 | `tone` `production` `identity_exposure` `forbidden_topics` `surface_rule` | **本人，数据差也不改** |

**永远不要因为「这样数据会更好」去改价值观层。**
复盘只对数据层提修改建议。

`tone` 里 `_internal_*` 开头的是**给自己看的原则，绝不说出口**；
`examples` 才是能念的话。两者混用是自动剪辑最容易犯的错——
总结性的原则句读起来最「点题」，也最容易被直接写进文案。

**十、记住的东西不要再问第二遍。**
风格、固定元素、口头禅、忌讳、验证过的有效做法——
一旦确认就写进 `profile.yaml`，**这是这个 skill 的长期记忆**。
下次直接用，不要重新问一遍。
复盘验证过的结论写回 `profile.learned`，让判断随时间变准。

**十一、人性不变，平台标准变。**
好奇、共情、身份认同在哪都成立；但小红书要的是「值得存下来」，YouTube 要的是「看得久」。
定位跨平台，封装分平台。见 `references/platform-playbook.md`。

**★ 十六、单维打分会把账号带偏。**

给选题排序时，**至少三个维度**：

```
流量  这条能跑多大        天花板
变现  对变现人设有没有帮助  生意
人设  累积起来把他塑造成谁  积不积极、像不像他想成为的人
```

真实失效记录：只按流量分反推，生成了一整组
「我最狼狈 / 我最后悔 / 我学不动了 / 我最不体面」——
**流量分全组第二高，人设分接近 0。**对卖服务的人，那是简历上的减分项。

**三个维度的冠军通常互不重叠 —— 单一维度的最优解不是全局最优。**
详见 `workflows/3-topic.md`「三维打分」。

**★ 十五、产出是替换式的，不是追加式的。**

每轮结束时，**被这一轮推翻的旧内容当场删掉或改写** —— 不留横幅、不留 vN、不留孤儿引用。

```
追加式   每轮加新结论 → 旧的等人发现才删 → 必然积垃圾 → 用户被迫「经常要清理」
替换式   每轮结束时当场收口 → 不积
```

**「先加上，回头再清理」不是效率，是把成本转移给下一轮。**

三条机械规则：**一个事实只写一处**（别处写引用）· **能算的数字不手写** ·
**引用用锚点不用章节号**。检查清单在 `9-deliver` 第四条硬标准。

**★ 十三、两件事同时做：了解这个人，和给这个人客观工具。**

编导的判断永远带主观。**所以每给一个结论，尽量同时给一个用户自己能算的判据** ——
他不必相信你，他可以自己验。

| ❌ 只有判断 | ✅ 判断 + 可自算的判据 |
|---|---|
| 「这条选题不够有趣」 | **对象测试**：这条里那个具体、外部、可辨认的东西是什么？填不上就不拍 |
| 「你太依赖个人经历了」 | **挑人度**：换个处境相同的人还能不能拍？通 / 半 / 独，数一数比例 |
| 「vlog 效果不好」 | **单位成本回报**：每成品分钟换来多少曝光？先控住时长再比 |
| 「标题要改」 | **同频道内对照**：拉自己 + 对标频道全量，同一频道内按句式分组比中位 |

**判据要满足两条**：① 用户手上的数据就能算；② 换个人算结果一样。
满足不了的，说清楚那是判断不是结论。

⚠️ 反面：**不要发明判据。**凡是「我推的」形式、命名、符号，
先在已验证清单里找；找不到就说找不到 —— 见十四条。

**★ 十四、本人要「一个东西」时，不要现场发明一个填进去。**

真实失效记录：用户要「视觉锚 + 口头禅」，编导给了五个元素，**四个是凭空发明的**（零出处），
当场被否。同类错误在同一个项目里发生过四次（系列名 / 推活动 / 自创符号）。

**标了「这是我推的」不等于规避了风险。**
正确做法：在 `learned` 与已验证清单里找 → 找不到 → **说找不到，并说明已验证的替代是什么**。

**十二、选题四来源，用了几个要说清楚。**

`3-topic` 规定四个来源：你是谁 / 观众要什么 / 平台在推什么 / 你拍得出什么。
每次产出选题，**逐一标注这四个来源的落实状态**：

```
✅ 已用   🟡 部分   ❌ 未落实（说明原因，如 MCP 不可用）
```

只用一两个来源就出选题，结果一定偏——**这是返工的主要来源，不是判断失误**。
用户有权知道这批选题是基于多少输入产生的。


**十七、事件激发表达欲 —— 长度和深度来自事件，不来自题目。**

```
事件型   事已经发生，观点是现成的，开拍前只需要回忆        ✅ 能高频
概念型   开拍前要现场想观点、搭结构                       🔴 每条都是一次小写作
资料型   开拍前要查资料、核数字                           🔴 定死低频
```

实测（218 条真实频道）：事件型中位时长 **353s vs 资料型 160s**，
≥5 分钟占比 **57% vs 9%**。**内容自己长出来，不是硬撑出来的。**

🔴 **但流量上不成立**（六频道横测 0.64×–9.23×，有反向的）。
**事件型换的是长度和准备时间，不是播放量** —— 别拿它当爆款理由。

⚠️ 这条决定的是**能不能持续更新**。给一个概念型的选题库，
用户会觉得「条条都对但拍不动」—— 因为成本被推到了开拍那一刻。
**产出选题时统计事件率，低于 50% 先改写再交付。**见 `3-topic` 闸门三。

**十八、选题尽可能系列化 —— 孤条只换收藏，不换关注。**

被验证的涨粉路径只有两条，**都是系列**：**模板路**（一个能连发 60 次的句式 + 形式，
实测 11,080 粉）和**连载路**（一条能追一年的真实进行中的事，实测 9,402 粉）。
第三种是**固定元素**（每条都出现的同一样东西）—— **只在封装层被验证**
（昵称格式、封面做法），内容层的道具 / 口头禅没有独立证据。

```
事件   把「想清楚一件事」的成本，从开拍那一刻挪到事情发生那一刻
系列   把「想清楚怎么拍」的成本，从每一条挪到第一条
```

**默认假设要反过来**：不是「这条能不能成系列」，是「这条凭什么是孤条」。
产出选题时统计**孤条率，>50% 先归并再交付**。

⚠️ **固定元素不能现场发明。**给用户设计一顶帽子、一句口头禅、一个固定收尾，
是编导最容易犯也最容易被当场否掉的错 —— 合法来源只有一个：
**这个人生活里已经每天在做的事**，从 `profile` 的刻板行为里挑，不从想象里挑。

**十九、调性是三个动词，不是形容词。**

`tone` 里填「真诚」「专业」「接地气」等于没填 —— **形容词在写脚本那一刻用不上。**
逼出动词，每个动词配一个反面：

| ✅ | 🔴 反面 | 差在哪 |
| --- | --- | --- |
| **分享** | 讲授 | 「我遇到了，给你看」不是「你不知道，我告诉你」 |
| **展示** | 宣布 | 「东西在这儿，你自己看」不是「结论是这样」 |
| **沟通** | 推销 | 「你那边呢」不是「找我」 |

**反面比正面好答** —— 问用户「你最不想变成哪种」，比问「你想要什么风格」有效。

⚠️ **资料型内容不是例外。**形态可以是口播干货 / 图文，调性照样是这三个动词：
**必须用大白话（术语当场翻译）、必须有例子（挂一件发生过的具体事）。**
一个账号连发 24 条术语词典换来 0 条真实咨询 —— 死在讲授 + 无例子，不是死在题材。

这条属于**价值观层**（原则九）：**数据差也不改。**

**二十、流量不积累，是突然来的 —— 能优化的只有下限。**

八个真实频道实测：**相邻两条播放量的自相关中位 0.06**（七个在 ±0.12 内）。
「上一条的真实成绩」这个信息量最大的预测源，对下一条只解释 **0.4% 的方差**。

```
下限型的号   中位 481,606 / 最高是中位的 11×
彩票型的号   中位     760 / 最高是中位的 1,341×
```

**八个号单调分开：做大的号不是彩票中得多，是下限抬得高。**

| 做什么 | 怎么做 |
| --- | --- |
| 打分 | 流量分 = **下限分**，不是预期播放量 |
| 排期 | 稳定输出下限高的，**爆款当彩票** |
| 复盘 | 看**中位数的移动**，不看最高值 |
| 「最近数据不好」 | 先看中位掉没掉。没掉 = 只是没中彩票，**不是账号出问题** |

⚠️ **爆款之后不要归因。**自相关 0.06 意味着成因大部分在内容之外
（分发抽样、时机、外部话题）。**把爆款拆成「成功公式」照着做，是在拟合噪声。**

⚠️ **抬下限和造爆款的处方会打架。**实测同一账号：标题用第一人称抬下限 **1.93×**，
却把峰值压到 **0.39×**。**先问用户要稳还是要爆，别默认两个都要。**

★ 所有能写成规则的东西 —— 对象、事件、系列、调性 —— **全部是抬下限的。
这不是巧合：下限是唯一可被规则控制的部分。**

**二十一、闸门决定能不能拍，分数决定先拍哪个 —— 两者不能混。**

```
闸门（过/不过，不进分数）   对象 · 事件 · 系列 —— 缺一条不拍
分数（只用于排序）           三维加权
```

⚠️ **把闸门折进分数，高分会盖住缺陷。**真实失效：一条流量分全组第二高的选题，
变现 0.05、人设 0.62 —— 分数把它推到了榜首附近，而它根本不该拍。
**一条 9 分但没有事件锚的选题，不拍。**

⚠️ **打分维度的权重要么来自实测，要么来自用户明说的优先级，不要自己拍。**
下限分的每一项权重都应能追溯到一个测出来的中位比；
「哪个维度权重最高」则是用户的决定（一个真实用户的答案是「生活最优先」）。

★ 配套动作：**选题库要能一键重跑**。写成脚本 + 一个 JSON，
改了选题重跑一次就出新排序 —— 手工重排必然跟不上迭代。

**二十二、闸门是「物」，不是选题；而且「能拿到」不等于「必须联系你才能拿到」。**

```
❌ 十五条选题配十五个交付物   做不完，每个都薄
✅ 4–5 个物，每个挂 2–4 条选题
```

挑物的三条判据：**意图纯度**（谁都想要的换不来买家，只换来伸手的人）·
**制作成本**（优先挑已经存在的）· ★★ **主角度**（最好的闸门是交付物就是这条内容的主角，
不需要过渡句）。

🔴 **平台松绑是个陷阱。**能在描述区放链接的平台，直接挂文件**下载量好看、lead 是零** ——
和「把表格放进图里」是同一个错误，换了个现代外壳。
**链接要指向一个留下联系方式的地方，不是文件本身。**

⚠️ 闸门数量受商业配比约束（`profile` 写「带闸门 1/10」就只该有 1/10），
装满会把账号推成竹竿型。**例外**：交付物本身不含商业话题时算生活侧，不吃职业配额。

⚠️ **补闸门前先查 `profile` 里已经有几个。**「缺」是需要核对的判断，不是印象。

**二十三、一句个人事实 ≈ 一条内容线。推断出不了选题，因为推断产不出「形状」。**

一个真实项目的对照：**基于 `profile` 推断的 367 条选题，三闸门合格 1 条（0.3%）；
用户陆续给出的个人事实，换来 65 条 12 线，合格 65/65。**

```
❌ 标签   「他从工程师转行做经纪」                     推断得出，出不了选题
✅ 形状   「某年某月，因为忙和父亲大吵一架，
           当晚算了遍现金流，第二天原地辞职」          必须问，一条线 9 集
```

**同一件事。**[闸门四（事件）](#)要的就是形状 ——
这就是为什么推断出的选题过不了事件闸门。

| | |
| --- | --- |
| **`profile` 记形状，不记标签** | 每条事实后面追问「**那具体是哪一天、发生了什么**」 |
| **推断一律标 `[推断]` 并写清推法** | 每一批新事实都会推翻一个推断 —— 不标就无法撤回 |
| 🔴 **引语不要写成事实** | 真实事故：一句别人说的话被记成了本人属性，差点让 agent 写出硬错误 |
| ★ **必问「有没有什么是你不能说的」** | 最容易跳过，回报最高 —— **限制本身经常就是最好的一集** |

⚠️ 这不违反原则七。原则七说的是「不要用表单挡住开工」，
**不是「永远靠推断」**。榨干已有信息之后，**问，而且问形状。**

**二十四、事实只存一处，而且存精确日期。**

**一 · 事实文档和方法文档分开 —— 理由不是整洁，是事实会漂移。**
真实事故：同一个年龄，`profile` 里记成引语、选题库里读起来像陈述，
**差点让 agent 把别人说的一句话当成本人年龄写进口播。**

```
profile    唯一事实来源 —— 谁、何时、多少、能不能说   （披露规则跟着事实走）
选题库      选题 · 判断 · 论证 · 数据依据             （事实一律引用，不复制）
```

一次真实清理删掉 **410 行**重复事实，选题和论证一行没少 ——
**那些行从来没提供过信息，只提供了漂移的机会。**

**二 · 收 `birthday: YYYY-MM-DD`，不收 `age: N`。**

| | |
| --- | --- |
| 约数会算错时间线 | 一个真实样本，用户口述的三个年龄**全部差一岁** —— 而连载的进度是靠时间线算的 |
| ★★★ **日期之间会产生关系，年龄不会** | `31 岁 / 6 岁` 什么都不产生；`01-20 / 01-20` **是同一天** —— 一条内容自己长出来了 |

**要找的关系**：同一天 · 相差一天 · 同月 · 与某事件重合 · 整十周年将至。
真实一例：收了精确生日才发现配偶和孩子同一天生日 ——
**真实、有情绪、每年自动复发、零制作成本，而收年龄永远发现不了。**

**二十五、叙事型内容线：先建时间线，再写选题。**

一个真实项目里，同一条内容线的性质**被改写了三次**，每次都因为补进了新的时间信息：
「干不下去了」→「刚证明能爬得比别人快，然后走了」→「铺了整整一年，那天只是扣扳机」。
**三次都不是事实错了，是事实不全** —— 而每次改写，**之前写好的钩子全部作废。**

| 规则 | |
| --- | --- |
| **一 · 表满了再写** | 判断标准：**这条线上每一年都能说出一件具体的事吗？**有空档先问，别用「大概那几年」糊过去 |
| ★★★ **二 · 导火索 ≠ 原因** | 用户会讲导火索（那是他记得最清楚的一幕），但**导火索单独讲，人物会变成一个冲动的人，而冲动的人不可信**。必问：**「那件事之前一年你在做什么」「如果那天没发生，你还会走吗」** —— 后一问的答案往往就是真正的第一集 |
| ★★ **三 · 顺序即论点** | 「怎么爬上去的」在前 → 离开读作**选择**；「为什么离开」在前 → 读作**逃离**。**顺序依赖必须显式写进选题库**，不能靠拍摄时临场判断 |
| ★ **四 · 问顺序，不只问内容** | 「他做某件事的顺序和大多数人相反」是最强的反差单点之一，**只有主动问顺序才会浮出来**。❌「你有什么」→ 清单；✅「**哪个是第一个**」→ 可能藏着反差 |

**配套**：不可分享的事实**往往是可分享事实的上游**。只从选题里删掉不够 ——
**观众会把话题推回去**。要给下游那条线加**护栏**（只讲可分享那段，不往回连）
和**预设止损答复**（提前备一句，不临场发挥 —— 被追问时人会本能多解释一句，
而那一句往往就越界了）。

**二十六、可信度靠反证，不靠证据。**

按常规做法，编导会去收集**支持论点的证据**（资产、成绩、作品）——
**收集得越多，看起来越像广告。**一个真实账号最后长成的是：

```
论点    因为做了 X，我才有了现在这种生活
物证    我确实有这些                  ← 支持
反证一  我的身体数据不同意             ← 生理层
反证二  我的钱是借来的，我还是必须工作   ← 结构层
```

**两个反证 + 一个物证 —— 这个账号就不是在卖梦。**
只给证据，观众得到「他很成功」（仰视 → 不联系）；
证据 + 反证，观众得到「**他这条路是这样的，代价是这些**」（对照 → **有人会来问**）。

**三个反证抽屉**：身体/数据层（**和自我感觉矛盾的客观数字**）· 结构层（成功背后的机制约束）·
时间层（那件事之后的实际生活 vs 当初以为的）。

⚠️ **反证必须是真的。**编造一个可爱的缺点（「我就是太拼了」）比不给反证更糟。

★★★ **上位概念：从「分享」升到「样本」。**
一个真实用户的动机是「希望我的故事能给观众一些启发」——
**分享 = 我遇到了给你看；样本 = 我是一个可以被研究的案例，包括代价和失败。**
**推论：样本必须包含代价，否则就是广告** —— 这是反证线的授权来源。
⚠️ 但样本 ≠ 教人怎么做。

**二十七、产出之后要留一个「离线批改」的入口。**

选题到几十上百条后，**每改一条都回来对话是不可持续的**。
一个真实用户：「我需要可以非常简单地 approve/disapprove，而不是每次都回来和你对话。」
**这不是效率问题，是所有权问题 —— 选题库是他的，他必须能在编导不在场时动它。**

```
选题.json（脚本写）  ·  选题审批.csv（用户只动两列）  ·  选题-当前.md（脚本生成的只读视图）
                     一条命令：apply → 写回 → 重算 → 归档「不拍」→ 刷新视图
```

**缺一条就会退化成又要对话**：CSV 不用 Markdown（Numbers 能排序筛选）·
决定列是枚举，**读到别的值直接报错不要猜** · 填「改」**硬性要求写备注** ·
`export` 保留已填决定 · 视图标注「不要手改」 · 「不拍」**移出归档但不删除，并带理由**。

★ 附带好处：**用户批完之后，从「他系统性地否掉了哪一类」学到的，比问「你喜欢什么风格」准得多。**

**二十八、三组最容易混的「形式 vs 内容」。**

编导最贵的返工都来自把形式层的东西当内容层来解：

| | 用户说 | ❌ 误读 | ✅ 实际 |
| --- | --- | --- | --- |
| **载体 ≠ 题材** | 「X 是我最好的连载载体」 | 去研究「X 类内容怎么做」 | **X = 每周 N 个录制窗口**，内容照旧 |
| **外壳 ≠ 结构** | 「用日记形式」 | 「今天…说说最近的想法」= 流水账 | 「今天…忽然想起某年那件事」= 存量内容换个挂法 |
| ★★ **内容 ≠ 时机** | 量出「低成本 × 持续供给 = 0」 | **发明一条新线去填** | **解耦**：内容是存量，触发是当下 |

判断方法：**「这件事会出现在内容里，还是只是你做内容的时间点？」**——一句话就能分开。
外壳的判据：**拿掉开场那两句，这条还完整吗？**完整=外壳，不完整=结构，退回。
★ **空格子不一定意味着缺内容，可能只是缺一个把存量挂到当下的封装。**

⚠️ 真实代价：因为「载体/题材」这一个误读，跑了两轮全平台采集 1,300+ 条，
**得出的全部结论都答非所问。**

**二十九、「发明」的三级递进，越往后代价越大。**

| 级 | 长什么样 | 代价 |
| --- | --- | --- |
| 一 · 发明一个形式 | 用户要「一个记号」，编导设计五个，四个零出处 | 被否就没了 |
| 二 · **为填自己量出的空格而发明** | 量出数据空缺，造一条线去填 | 🔴 **自欺** —— 有数据背书，看起来像有依据 |
| 三 · **越伦理边界** | 提议把用户的私人社交场合当素材来源 | 🔴🔴 **代价落在用户的关系和信用上，不可逆** |

**原则十四只挡得住第一级。**加两道：
**量出空格 ≠ 授权发明**（空格只说明该去问，不说明该去造）·
**任何涉及第三方的内容源，先问「这个人知道并同意成为素材吗」**（不知道 = 不用，不管数据多好看）。

★ **找不到答案时，正确做法是说「这个需求可能不成立」** ——
把用户一周逐项过完并全部被否，**那不是搜索失败，是一个结论**。
但说「不成立」要拿出三条理由，否则只是放弃。

★ **能用物理约束表达的规则，不要用约定表达。**
「固定镜头放片头别进叙事」（约定：要记、会忘）→「固定镜头**不出现人**」
（物理：人一入镜画面就带当天信息，复用立刻露馅，**不可能违反**）。
写下一条规则后问：**「有没有一个物理事实能让违反它变得不可能？」**
⚠️ 这条是用户提的 —— **用户更了解自己的物理约束。**

**三十、分享一份内容文档时，该脱的不是个人事实，是方法。**

用户要把选题库分享给别人时，**本能反应是去脱敏个人事实** ——
一个真实项目里我第一步就去数「多少条选题涉及财务/家庭/位置」，量出 36%。**那是错的问题的答案。**

```
选题本身    = 他迟早要公开发布的   → 别人先看到，无所谓
真正私密的  = 他怎么决定发什么     → 打分 · 配额 · 转化设计 · 论点结构 · profile 引用
```

★ **反直觉**：个人事实是**要公开的内容**；**决策依据才是私密的** —— 编导的本能刚好反过来。

| | |
| --- | --- |
| **分享版必须脚本生成** | 手工另存 = 必然漂移（同原则二十四） |
| ★ **泄漏扫描要分层** | 一次真实扫描命中 13 处「敏感词」，**全是选题正文里的词** —— 那是内容不是泄漏。**按「它出现在哪一层」判断，不是按「有没有出现」** |
| ⚠️ 标出有前置条件的条目 | 等某人同意 / 带着不可讲的上游 —— **不标等于默认它们可以拍** |

★ 配套（`11-audit`）：**陈旧文件比死链危险。**死链会报错，陈旧内容不会 ——
一个真实审计里仓库全绿（0 死链、闸门全过、禁区 0 命中），
但三个「给人看」的文件**全部停留在上一代编号体系**。
加两条检查：**编号存活**（抽出所有编号回数据源查还在不在）·
**「更新于」比主数据源旧一个版本以上就挂起**。处置是**归档 + 顶部横幅指向替代物，不是删**。

**三十一、了解用户这一环：疯狂追问，榨干为止 —— 停得太早是最大的失效模式。**

**这是整个 skill 里唯一一个「问得越多越好」的环节。**

一个真实项目：基于 profile **推断**产出 367 条 → 合格 **1 条（0.3%）**；
**追问 20 轮**换来 100 条 18 线 → **合格 100/100**。
**而最要命的四条全在第 14 轮之后** —— 按原则七「只问缺了就做不下去的」，前三轮就停了。

🔴 **最该早问、却最容易漏的一个：「你上次为什么停 / 现在什么拦着你？」**
真实教训：这个问题到**第 16 轮**才问出来，答案是「帮我剪辑的人辞职了」——
**在那之前 15 轮，一直把断更当成「没选题」在解，做了 99 条选题去解决一个不是选题的问题。**
**瓶颈问题必须第一个问：它不改变你能做什么，它改变什么算做完了。**

**追问顺序**（按这个问能省十几轮）：
① 上次为什么停 · ② **上周流水账**（要流水不要总结 —— 形状藏在重复里）·
③ 有没有什么不能说的 · ④ 最近一次转变「那一天」发生了什么 ·
⑤ 那件事**之前一年** · ⑥ 你有什么？**哪个是第一个** · ⑦ 生日不是年龄。

**怎么问**：顺着他刚说的往下挖一层 · 一次一个 ·
★ **最高产的回答往往不是他回答你问的那个，是他因为被问到而主动补的下一句。**

**什么时候停**（三个信号同时出现，不是「问够了」）：
他开始重复 · **新事实不再推翻任何已有判断**（最可靠的那个）· 时间线每一年都有具体的事。
⚠️ 真实项目里前 12 轮每一轮都推翻一个既有推断 —— **只要还在推翻，就没问完。**

⚠️ **和原则七不冲突**：原则七管的是「开工前不要用表单挡路」，不是「全程少问」。
**边做边榨，不是先榨再做。**

**三十二、从评论区挖选题 —— 但不要用点赞数筛，用回复数。**

`3-topic` 四来源里「**观众要什么**」最常被跳过，因为它听起来像「多读评论」而不是一个动作。

```
自己的评论区   告诉你「你的观众是谁」   —— 断更时是干的（一个真实账号 170 条评论只有 1 条真实需求）
别人的评论区   告诉你「市场在问什么」   —— ★ 任何时候都能采（32 篇他人笔记 → 580 条评论）
```

⚠️ **两批引用时句式不同**：自己的可以说「有人问我」；
**别人的只能说「这个问题我看到很多人在问」——说「有人问我」就是编造来源。**

🔴 **不要用点赞数筛。**实测 424 条评论：**提问型中位赞 0**，非提问中位赞 1 ——
因为点赞是给「说得对」的，**想问的人用回复表达**（「蹲」「+1」）。
实例：`0 赞 4 回复` 的一条真问题，**用赞筛会直接丢掉**。

**三个筛子**：★★★ **回复数**（原生信号）· ★★ 赞数 ÷ 该篇总赞（去掉大号混淆）·
★ 同篇内相对排名（去掉位置效应）。**都不要用绝对值。**

**挖到之后的判据 = 事件闸门**：能用亲身经历答 ✅ 做 · 要查表 🟡 资料型低频 ·
**「报价多少/有没有房源介绍」🔴 跳过 —— 那是找服务不是问问题，答它就变成广告。**

★ **回复优质评论比挖问题更强**：「有人说 Y，我不同意」——
**有对象 · 有对手 · 有事件锚，三道闸门一次全过。**

**做成定期，但别挂 cron** —— 需要登录的平台 cookie 会过期，**挂了会静默失败**。
一条命令 + 状态文件（指纹去重、断点续跑、只报新增），没登录就停下并打印重登方法。
⚠️ **采集时务必存来源 ID**（真实教训：580 条采完没存 note id，回原帖核对都找不到）。

⚠️ **平台能力不对等**（见 README 表）：读自己账号数据 YouTube 白拿、
小红书要人工截图录入；对标调研 YouTube 强得多；评论密度小红书更高；发布只有小红书能。

**三十三、封面和开头是点击驱动平台的第一、二关 —— 而且封面必须看图，不能只看数。**

小红书和 YouTube 都靠点击，抖音是信息流。**别把抖音的逻辑套到前两个上**（那会让人把封面排到很后面）。

```
1 封面+标题 → 2 开头 3–15 秒 → 3 选题 → 4 关键词/描述 → 5 b-roll（几乎不影响前四关）
```

**封面分析法**：同频道内取最高 / 最低各 N 条，**各拼成一张图用眼睛看** ——
「平均色相」「文字占比」这类指标说不出差别在哪，拼出来一眼就见。⚠️ 必须同频道内比、必须剔 Shorts。

两个同题材频道实测：**TOP = 一张大脸（正面·有表情）+ 超大字压在上面 + 高对比，实物只做背景；
BOT = 纯物件照 / 两个人对谈的小脸截图 / 同一底图套字**。

⚠️ **「模板封面」≠「固定版式」**：前者同一张底图只换字（死亡组合，实测一个号 58 条这么做全号中位 2 赞）；
后者**字体/颜色/位置固定、图每条不同** —— **一致性由字承担，识别度由图承担**。高分频道走的都是后者。

★★ **同一个元素，位置决定正负**：**问号结尾放标题 0.78× / 0.68× / 0.82×（三方一致为负）；
问句放封面，某频道 top 12 张里占 6 张。**→ **问题放封面和口播开场，不放标题。**

🔴 **开头必须读字幕，否则所有对标结论都停在标题层。**
YouTube Data API **拿不到字幕**（要 OAuth + 视频所有权）—— 用 `youtube-transcript-api` 补。
★ **跨平台还原**：小红书拿不到内容本体，但**同一博主的同一条内容常常也发在 YouTube** ——
在小红书看到爆款 → 找他的 YouTube → 拿字幕 → 读到内容本体。目前唯一的办法。
★ 模板路也只有在字幕里才显形（某频道 46 条同一句式开场，标题层看不出来）。
---

## 环节地图

**统一入口可以直达任何环节。**先判断用户在问什么，再读对应文件。**不要一次读全部。**

### 需求澄清

| 情况                   | 读                      |
| ---------------------- | ----------------------- |
| 信息不足，不知从何问起 | `workflows/0-intake.md` |

### 账号与人物

| 情况                         | 读               | 产出                        |
| ---------------------------- | ---------------- | --------------------------- |
| 这个号该怎么做 / 定位 / 起号 | `1-account.md`   | `profile.yaml`              |
| 「我很普通，没什么好拍的」   | `1b-discover.md` | 候选支柱清单                |
| 已有账号和数据，想提效       | `1c-backfill.md` | 反推 profile + 断层报告     |
| 已有账号且有 MCP 工具        | `1d-scan.md`     | 自动扫号 → 同上             |
| 人设怎么立 / 凭什么被信      | `2-ip.md`        | `profile.yaml` 的 ip 段     |
| 我有好几种内容形态           | `8-styles.md`    | `profile.yaml` 的 styles 段 |

### 内容决策

| 情况                    | 读           | 产出                    |
| ----------------------- | ------------ | ----------------------- |
| 拍什么 / 这条选题行不行 | `3-topic.md` | `topic-card.yaml`       |
| 标题 / 封面 / 开头      | `4-hook.md`  | `topic-card` 的 hook 段 |

### 制作

| 情况                        | 读            | 产出                               |
| --------------------------- | ------------- | ---------------------------------- |
| 脚本怎么写 / 怎么拍 / 分镜  | `5-script.md` | `script.yaml`                      |
| 怎么剪（自己剪 或 交给 AI） | `6-edit.md`   | `edit-plan.json` / `edit-sheet.md` |
| b-roll + 配音，想全自动     | `6b-autoedit.md` | `edit-plan.json` + 搭好的时间线 |

### 反馈与成长

| 情况                       | 读               | 产出                |
| -------------------------- | ---------------- | ------------------- |
| 数据不好 / 复盘 / 怎么优化 | `7-review.md`    | 诊断 + 回流指令     |
| 想自己学会，不只要成品     | `10-training.md` | 前后对照 + 验收标准 |
| 按流程重新审视已做的东西 / 怀疑没走流程 | `11-audit.md` | 审计报告 + 修正清单 |

### 出口（所有分支必经）

|            | 读             |
| ---------- | -------------- |
| 交付前验收 | `9-deliver.md` |

---

## 横向跳转

环节之间不是只能顺序走。常见的合法跳转：

```
1-account  →  2-ip        定位定完立刻立人设
1-account  →  3-topic     定位定完直接出选题
2-ip       →  3-topic     人设决定能讲什么
2-ip       →  5-script    人设决定怎么讲
3-topic    →  4-hook      选题定完立刻做钩子
3-topic    →  5-script    钩子已有，直接进脚本
4-hook     →  5-script    常规路径
8-styles   →  5-script    风格决定脚本结构
8-styles   →  6-edit      风格决定剪辑参数
```

**规则**：任何环节发现上游文件缺失或与当下矛盾，**先回上游**，不要凑合往下做。

---

## 四种循环（必须走，不是可选）

### 循环一 · 验收不过 → 回原环节

`9-deliver` 三条标准任一不满足 → 说明缺什么，回对应环节补，不交付半成品。

### 循环二 · 复盘 → 回上游

`7-review` 的产出**必须是回流指令**，不能停在「内容要更好」：

```
封面点击率差   → 4-hook
前3秒跳出率高  → 4-hook
完播率差       → 5-script
涨粉/播放低    → 3-topic
私信/咨询少    → 1-account
漏斗是雨伞型   → 1-account 重做商业定位
```

### 循环四 · 粗剪 → 等人 → 继续改
剪辑环节是硬停：粗剪完成后必须交接并等用户裁决，
说「通过」才进渲染，说「要改」就留在本环节改完再停。见下方剪辑闸门。

### 循环三 · 训练 → 补薄弱能力

同一类问题反复出现 = 能力问题，不是单条内容问题。
`10-training` 识别薄弱项 → 回对应环节做一次带前后对照的重做 → 下次观察是否改善。

---

## 剪辑闸门（唯一的硬停）

**粗剪完成 → 停下来 → 等用户决定。**

流程别处缺信息可以推断着往下走（原则七）；**这里不行**。
剪辑是第一个产出成品形态的环节，再往下（渲染、发布）都不可逆。

```
AI 剪到粗剪  →  交接：做了什么 / 不确定什么 / 没做什么  →  等
                                                         ├─ 通过 → 渲染、发布
                                                         └─ 要改 → 留在本环节，改完再停一次
```

| 规则 | 说明 |
|---|---|
| 不自行渲染 | 用户明确说通过才渲 |
| 改就留在本环节 | 循环直到通过，不要改完就往下冲 |
| 一次交代清楚 | 别反问一堆，让用户一句话就能回 |

详见 `workflows/6b-autoedit.md` 第十节。

---

## 四种用户，四条入口

**先判断用户属于哪种，不要用同一条路径对待所有人。**

| 用户状态 | 入口 | 为什么 |
|---|---|---|
| **零基础**（没账号、说自己很普通） | `1b-discover` → `1-account` | 「你有什么」问不出来，得先挖事实 |
| **有账号 + 有 MCP 工具** | `1d-scan` → 按断层切入 | 能自己读的就别问，读完只补三个问题 |
| **有账号 + 无工具** | `1c-backfill` → 按断层切入 | 同样的判断逻辑，改成让用户贴数据 |
| **中间态**（有想法没成体系） | `0-intake` → `1-account` | 常规路径 |

**这四条不是隔离的。**用户可能同时是「有小红书号」和「YouTube 零基础」——
那就小红书走 `1d-scan`，YouTube 走 `1-account` 补平台适配。
真人编导不会因为用户不属于任何一类就卡住。

---

## 开工前六查

1. **有 `profile.yaml` 吗？** 没有 → 按上表选入口。
2. **有 `styles` 段吗？** 没有且用户有多种内容形态 → 走 `8-styles`。
3. **本次任务在哪个环节？** 不确定 → 走 `0-intake`。
4. **上游文件齐吗？** 缺的能推就推、标 `inferred`；只有「缺了做不下去」才回上游。
5. **发哪个平台？** 不知道就先问一句——平台决定标题长度、时长、成败判据。
6. **有 MCP 工具吗？** 有就先读再问（`references/mcp-tools.md`），探测失败直接转人工。

---

## 方法论索引

⚠️ **前两份不是「参考资料」，是判据的来源 —— 对应环节必读，不是「需要时再读」。**

| 文件 | 什么时候**必读** | 它提供的判据 |
|---|---|---|
| **`references/method-viral.md`** | **`3-topic` · `4-hook` · `7-review` 每次** | 爆款四条件 · 三类型 · **作品目的五型** · 涨粉两条路（模板路/连载路）· 未完结叙事 · 分镜 |
| **`references/method-account.md`** | **`1-account` · `3-topic` · `7-review` 每次** | 八标签 · **脚本结构六种** · **呈现形式四选** · IP 五要素（含最常被跳过的**道具**）· 漏斗四形态 · 转化=闸门 |

其余按需读：

| 文件 | 内容 |
|---|---|
| `references/hot-and-language.md` | 热点判断、语言习惯、平台差异 |
| `references/platform-playbook.md` | 平台评判标准与封装差异 |
| `references/mcp-tools.md` | 可用的 MCP 工具、各环节怎么用、退化规则 |

### ★ 为什么改成必读

**原来这里写的是「需要时再读，不要预读」—— 实测结果是从不读。**

后果不是「少读了两份文档」，是**判据缺失而不自知**：
`11-audit` 的检查清单只列了「闸门 + 爆款元素 + 四条件 + 四来源」，
于是**按清单查是合格的，按方法论查缺三项**（作品目的 / 脚本结构 / 爆款类型）。

> **清单是方法论的摘要，不是替代品。清单没写的，不等于不用做。**

模板在 `templates/`，产出文件时照模板结构填。

---

## 发布与记账层

创作之外的事（分发、记账、配额、排期）在 `automation/`：

| 文件                              | 内容                           |
| --------------------------------- | ------------------------------ |
| `automation/spec.automation.yaml` | 平台能力、配额、节奏、发布策略 |
| `automation/ledger.schema.yaml`   | 发布履历完整结构               |
| `automation/agents.md`            | 自动化 agent 的职责边界        |

**`7-review` 的漏斗诊断依赖 ledger。**没有 ledger 只能做单条定性诊断。

⚠️ 两层共用字段名。改任一层前查 `docs/sync-notes.md`。
