# 更新日志

## 0.17.0 — 2026-08-10

### 十九 · 调性是三个动词，不是形容词（`SKILL.md` · `5-script` 新章 · `8-styles` 零节）

用户原话：「账号的调性：分享、展示、沟通。即便是资料型的也要用这种方式，
比如必须用大白话，要有例子。」

`tone` 里填形容词（真诚 / 专业 / 接地气）等于没填 —— 写脚本那一刻用不上。
动词能逐句对照，且每个都有明确反面：

| ✅ | 🔴 反面 |
| --- | --- |
| 分享 | 讲授 |
| 展示 | 宣布 |
| 沟通 | 推销 |

**反面比正面好答** —— 问「你最不想变成哪种」比问「你想要什么风格」有效。

新增：**资料型不是例外**。形态可以是口播干货/图文，调性照样是三个动词 ——
必须大白话（术语当场翻译）、必须有例子（挂一件发生过的具体事）、
不许只有结论（「一般来说」单独出现 = 退回）。

实证：一个账号连发 24 条术语词典换来 **0 条真实咨询** —— 死在讲授 + 无例子，
不是死在题材。同一知识点换姿势即可同时满足调性与事件闸门。

归入**价值观层**（原则九）：数据差也不改，复盘不得对它提修改建议。

## 0.16.0 — 2026-08-10

用户提出两条，均已验证并入库。

### 十七 · 事件激发表达欲（`SKILL.md` · `3-topic` 闸门三）

用户原话：「能够让内容有一定长度和深度的，永远是事件激发表达欲，能够发表个人的
经验、感想、思想。这样才可以减少准备时间。」

实测 218 条真实频道，按标题能否指认一件发生过的事分组：

| | n | 中位时长 | ≥5 分占比 | 每千曝光评论 | 零评论率 |
| --- | --- | --- | --- | --- | --- |
| 事件型 | 88 | 353s | 57% | 10.55 | 36% |
| 资料型 | 11 | 160s | 9% | 7.56 | 36% |
| 概念/状态型 | 119 | 190s | 27% | 8.74 | 47% |

- 长度 ✅ 干净且大（2.2× / ≥5 分占比 6.3×），剔 <60s 后更大
- 深度 🟡 弱同向（1.22–1.40×）
- 流量 ❌ **不成立**，六频道横测 0.64×–9.23×，有反向的 —— 不要拿它当爆款理由
- **真正的理由是准备时间**：事件型开拍前只需回忆，概念型每条是一次小写作

新增：概念/状态型**默认不产出**，除非能挂上 事件 / 经验 / 能激发分享 至少一项。
新增：选题库要统计**事件率**，<50% 是拍不动的库。

### 十八 · 选题尽可能系列化（`SKILL.md` · `3-topic` 新章）

把 `method-viral` 的两条涨粉路径（模板 11,080 粉 / 连载 9,402 粉）提为选题期默认，
并补第三种「固定元素」——**标明只在封装层被验证**（昵称格式、封面 29/30），
内容层的道具/口头禅无独立证据。

- 默认假设反过来：不是「这条能不能成系列」，是「这条凭什么是孤条」
- 新增**孤条率**指标，>50% 先归并再交付
- 模板要能连发 60 次才算模板 —— 开库时列满 20 条，列不满就换
- 连载在订阅制平台会单调衰减（实测 280→116），三硬前置升为硬闸门
- ⚠️ **固定元素不能现场发明**（原则十四特例）：只能从 profile 的刻板行为里挑

### 两条的共同机制

```
事件   把「想清楚一件事」的成本，从开拍那一刻挪到事情发生那一刻
系列   把「想清楚怎么拍」的成本，从每一条挪到第一条
```

## [0.14.0] - 2026-08-10

### 三维打分：流量 · 变现 · 人设

**闸门决定「能不能发」，打分决定「先发哪条」。**此前 skill 只有闸门，没有排序工具。

```
综合分 = 流量 × 0.4  +  变现 × 0.3  +  人设 × 0.3     三项各 0–10
```

| 维度 | 问什么 | 主要成分 |
|---|---|---|
| **流量** | 能跑多大（天花板） | 群体大小 + 爆款类型 + 有没有对象 + 标题因子 − 执行性扣分 |
| **变现** | 对变现人设有没有帮助 | **闸门（权重最大）** + 离生意多近 + 是否筛出潜在客户 − 招同行 |
| **人设** | 累积起来把他塑造成谁；积不积极 | 离「他真实在过的好日子」多近 − 负面/自贬词 + 正向词 |

#### 🔴 加第三维的原因：只按流量打分，会把账号带偏

真实失效记录：给一个**服务型**账号打完流量分，再按分数反推生成新选题 ——
结果是一整组 **「我最狼狈 / 我最后悔 / 我学不动了 / 我最没底气 / 我这行最不体面」**。

**流量分全组第二高（7.40），人设分 0.62。**
对一个卖服务的人，**这不是人设，是简历上的减分项** —— 而流量分完全看不见这一点。

**第三维的本质是 `W009 人格是副产品` 的打分化：
内容不是描述人设，是塑造人设。**

#### 三条实践规则

| # | |
|---|---|
| 1 | **三个维度的冠军通常互不重叠** —— 实测流量冠军是「大群体+地名」组、变现冠军是「晒过程+闸门」组、人设冠军是「他真实在过的日子」组。**单一维度的最优解不是全局最优** |
| 2 | **人设分 ≤2 的内容每 10 条最多 1 条**，且必须有收束（「后来我改了什么」），不能停在惨上 |
| 3 | **分数必须能重算** —— 每个系数指向一条可查实测，组级人工项单独列出。做成脚本，改了选题就重跑，不要手改 |

⚠️ 不要用综合分决定「删不删」，只用它决定「同一格里先发哪条」。
低分往往是**服务另一层漏斗**的内容。

新增 `SKILL.md` 原则十六。

---

## [0.13.0] - 2026-08-10

### 产品类型直接决定形态（来自「人类最强编导」）

用户补充的两条规则：

```
产品是服务型  →  拍 vlog
产品是资料型  →  拍口播干货 或 图文
```

**这一条比「哪种形态数据好」优先级更高**，写进 `method-account` 商业定位一节
与 `8-styles` 的第零节。

| 产品 | 卖的是 | 形态 | 打漏斗哪一层 |
|---|---|---|---|
| **服务型**（咨询 / 经纪 / 律师 / 教练） | 「跟这个人合作是什么感觉」 | **vlog** | **信任** |
| **资料型**（课程 / 模板 / 报告 / 工具） | 信息本身 | 口播干货 / 图文 | 流量 + 收藏 |

#### ⚠️ 同时记录一次真实误用（我犯的）

一个**服务型**账号，我按「每成品分钟换来多少曝光」算 vlog 的 ROI ——
41 vs 145，判定「vlog 用同样的效果收了 3.5 倍的价」，建议少做。

**数据没错，结论错了。**

```
资料型的产出   曝光 / 收藏          —— 每分钟 ROI 是对的度量
服务型的产出   看完的人愿不愿意找你   —— 每分钟 ROI 完全测不到
```

**用曝光衡量信任层的内容，必然得出「它不划算」——那是度量错了，不是内容错了。**
顺序反了，服务型账号会被自己的流量数据劝退到只剩干货，
**而干货恰恰是不转化的那一类**（「转化 = 闸门」：抽象干货换来「学到了」，不换私信）。

#### 补充：服务型 ≠ 叙事型长 vlog

真正的维度不是「vlog 还是口播」，是**口播在哪拍、场景给不给信息**。

```
场景口播（vlog 型）  A-roll 在一个具体地方说，场景本身带信息   成本 ≈ 桌面口播   ✅ 主力
桌面口播            无场景                                最低           🟡 只够讲情绪
叙事型长 vlog       全程剪辑                              最高           留给真事件
```

同账号实测：**5–10 分（多为场景口播）中位曝光 774 · 每千曝光评 3.66 · 转发 2.39；
≥15 分（叙事型长 vlog）469 · 2.54 · 1.98** —— 三项全面更高，成本只有零头。

**服务型的正确落法是「口播都尽量在场景里拍」，不是「多拍长 vlog」。**

---

## [0.12.0] - 2026-08-10

### 交付加第四条硬标准：内部一致

用户原话：**「skill 里面的引导和方法应该可以获得一致性的结果，而不是像我们现在这样经常要清理。」**

真实失效记录（同一项目，一轮之内）：文件 39 → 9 → 8；一份文件的条目
140 → 256 → 216 → 202 → 196，累计弃用 166 条；章节删 4 节重排产生 **22 处死引用**；
同一个总数在不同段落出现过 **5 个版本**。

#### 根因：产出模式是追加式的

```
追加式   每轮加新结论 → 旧的等人发现才删 → 必然积垃圾
替换式   每轮结束时，被推翻的当场删或改 → 不积
```

**「先加上，回头再清理」不是效率，是把成本转移给下一轮。**

#### 新增

| 位置 | 内容 |
|---|---|
| `9-deliver` **第四条硬标准** | 内部一致 + 三条机械规则 + 交付前 5 项检查（能脚本化就脚本化，别用眼睛） |
| `SKILL.md` **原则十五** | 产出是替换式的，不是追加式的 |

三条机械规则：
1. **一个事实只写一处**，别处写引用不写副本 —— 副本必然与本体脱节
2. **能算出来的数字不要手写** —— 手写的计数一变就过时
3. **引用用锚点，不用章节号** —— 章节会重排，锚点不会

⚠️ 同时写死：**加「本节已失效」横幅是权宜，不是交付。
超过一轮还挂着横幅的章节，直接删。**

这一条是原则十三（给客观工具）用在自己产出上 ——
**一致性不该靠回头审，该靠交付前跑一遍能跑的检查。**

---

## [0.11.1] - 2026-08-09

### 爆款四条件④ 补一节：怎么问，决定有没有互动

「提问式内容好不好」问不出答案 —— **三种问法效果完全相反**（四个账号，同账号内对照）：

| 问法 | 小账号 | 对标 A | 对标 B | 对标 C（娱乐大号） |
|---|---|---|---|---|
| 标题问号结尾 | **0.78×** | **0.68×** | **0.82×** | 2.04× |
| 开放提问（怎么 / 为什么） | 0.94× | 1.50× | 1.09× | 1.20× |
| **二选一（还是 / vs / 值不值）** | n=1 | **2.68×** | **1.77×** | 1.28× |

**垂类三家的问号结尾全负，只有娱乐大号正 —— 不要跨类型抄。**

新增区分：**「高曝光」和「有效评论」不是一回事。**
问号结尾曝光掉 22% 而评论率完全不变（3.17 vs 3.17）；
标题带「你」评论率 1.8× 但曝光掉 23%、转发掉到 1/3。

机制与缺口式实测同向（「扣 1」0 人执行 / 「你漏了 XX」自动长出）：
**观众不会替你组织答案，只会替你选一边。**

---

## [0.11.0] - 2026-08-09

### 两个可以自己算的闸门 + 编导的第二职责

用户原话：**「一边要了解博主，一边要给博主提供客观工具。」**
以及：**「理论上好的选题是不挑人的，我的表达会塑造网络上的我。」**

后一句其实是本项目 `learned` 里早有的结论（变量放在信息上，不是放在人上），
但方法论里**没有可操作的判据** —— 这一版补上。

#### 新增两个闸门（`3-topic` 选题卡最小字段集）

| 字段 | 判据 | 硬度 |
|---|---|---|
| **对象** | 这条里那个具体、外部、可辨认的东西：一个数字 / 一个地方 / 一群人 / 一件物 / 一个变化 | **填不上 → 不拍** |
| **挑人度** | 换个处境相同的人还能不能拍：通 / 半 / 独 | 独不能当主力配额 |

**对象测试的证据**：一个真实小账号 111 条，头部 7 条**全部有对象**，
底部（≥5 分）7 条**全部没有**。而头部那 7 条**长度从 18 秒到 8 分都有，vlog 与非 vlog 都有**
—— **形式不是变量，有没有对象才是。**
它与 `method-account` 的转化触发条件是同一条，那一节讲它决定转化，这里补：**它同时决定分发。**

**挑人度**把 `method-viral` 第七节的「IP 型 vs 内容型」变成逐条可判。
实测：个人故事簇中位评率是全站 **4.1 倍**，但中位曝光在最低几档。
```
挑人的内容    互动质量高，分发差
不挑人的内容  分发好
```
处方不是二选一：**用不挑人的选题 + 这个人自己的表达。**

#### 新增：单位成本回报（`method-viral`）

**只比「哪个效果好」会得出错的结论。**同一账号把成品时长当成本代理：

| | 每成品分钟曝光 | 每分钟转发 |
|---|---|---|
| vlog | **41** | 0.06 |
| 非 vlog | **145** | **0.36** |

而**控住时长之后两者中位曝光几乎相同**（517 vs 538；674 vs 652）。
→ 不是「vlog 效果不好」，是「**vlog 用同样的效果收了 3.5 倍的价**」。
⚠️ 时长甜点区**要自己测**，不要抄别人的数字。

#### 新增两条核心原则

| # | |
|---|---|
| **十三** | **两件事同时做：了解这个人，和给这个人客观工具。**每给一个结论，尽量同时给一个用户自己能算的判据 —— 他不必相信你，他可以自己验。判据要满足：①用他手上的数据就能算 ②换个人算结果一样 |
| **十四** | **本人要「一个东西」时，不要现场发明一个填进去。**真实记录：用户要「视觉锚 + 口头禅」，给出五个元素**四个是凭空发明的**，当场被否。同类错误在同一项目里第四次。标了「这是我推的」不等于规避了风险 |

---

## [0.10.1] - 2026-08-09

### 自我修正：v0.10.0 有三条是「单平台样本当成了通用规律」

v0.10.0 的证据全部来自**一个订阅制视频平台**。同一作者**同一批内容**
在推荐制平台的数据补上来之后，三条要改。**同期该平台曝光量是前者的 8.5 倍 ——
也就是说 v0.10.0 是拿副战场的数据在下通用结论。**

| 条目 | v0.10.0 | v0.10.1 |
|---|---|---|
| **合集换收藏，单点换评论** | 「机制不是形态，是分发」—— **判反了** | **原方法论是对的。**有收藏数据的平台复测：合集每千曝光收藏 **4.84** vs 单点 **2.00**（2.4×），评论 2.30 vs 2.64。之前判反是因为**那个平台没有收藏数据，分母里少了合集真正换到的那部分** |
| 未完结叙事被证伪 | 「未完结本身不产生追更」 | 降级为**平台相关**：同一个系列在订阅制平台单调衰减（280→116），在推荐制平台**没有衰减**（883→1074→673→676→1021）。订阅制的第 N 集只推给已订阅者，池子只会缩小 |
| 短视频播放是稀释的 | 当通用规律写 | 加平台限定：推荐制平台复测 <60 秒每千曝光评论 **3.02** vs ≥60 秒 **2.52**，**短片反而略高**。先问「这个平台的短视频是不是单独一个流量池」 |

**新增可操作指标：藏 / 评比 —— > 2 是合集型，< 1 是单点型。**

> **教训（同时写进本项目的工作方式）：**
> 一个作者在两个平台发同一批内容，**中位曝光差 4 倍，头部差 200 倍**。
> 拿单平台数据修方法论，等于拿一个平台的分发规则去改所有平台的创作规则。
> **修方法论之前，先确认这个平台是不是这个账号的主战场。**

---

## [0.10.0] - 2026-08-09

### 第一次用真实账号反向检验方法论

此前两份方法论的证据**全部来自外部采样**。这一版拿一个真实小账号的
**218 条全量数据（含真实播放量）+ 逐条读完的 170 条评论原文**去逐条检验，
判 ✅ 验证 / 🟡 需修正 / ❌ 证伪 / — 不可测。

**结果：8 条验证、5 条需修正或加条件、1 条证伪、3 条新增。**

#### ✅ 被真实账号复现的

| 主张 | 复现 |
|---|---|
| **转化 = 闸门** | 218 条、近 7 万播放，只换到 **1 条**真实商业需求，来自一支 **156 播放**的个人视频；24 条专业术语内容 + 16 条市场分析 = **0** |
| 索取型 / 定位型的触发条件是「具体可辨认的对象」 | 12 条问/索取型的触发物**全部**是具体地点 / 身份 / 事件 / 对方自己的资产，**无一来自抽象内容** |
| 并列结构不涨粉 | 24 条并列内容，中位 57 播放、71% 零评论，五个月共换 1,501 播放 |
| 运营漏斗诊断 | 竹竿型确诊，处方与独立算出的结论一致 |

#### ★ 系列名一节新增最强样本

同作者、同一天、同一主题，**唯一变量是编号**：
五条带编号**评论全部为 0**，同日不带编号那条 **8 条评论**，而播放没有系统性差别。
此前全是跨账号相关性，这是第一个控制住混淆项的对照。

#### 🟡 修正

| 原表述 | 修正 |
|---|---|
| 合集换收藏，单点换评论 | **单位播放的评论意愿几乎一样**（每千播放 9.64 vs 9.55）。差距全在分发端 —— 合集中位播放只有单点的 41%。处方从「别做合集」改为「合集的钩子拿不到点击」 |
| Top 10% 贡献 57.8% / 最高赞 726 倍 | 加边界：这是**有过爆款的账号**的特征。从未跨圈的小账号实测 40.6% / 35 倍，**分布更平本身就是诊断信号** |
| IP 五要素 · 道具 | 加条件：**自带外部流量池的道具会把那个池子的人带进来**。实测该簇中位播放全场第一、赞率全场倒数第一。**道具解决「认出你」，不解决「想跟你说话」** |

#### ❌ 证伪

**「未完结叙事让人追更」—— 未完结本身不产生追更。**
两个未完结系列实测单调衰减（280→116；57 赞→11 赞，−81%）。
新增三个前置硬件：**有终点 · 有对手 · 有可验证进度**。缺任意一样，
「未完结」只是「没剪完」。

#### 新增

| 新增 | 位置 |
|---|---|
| **短视频池的播放是另一种货币** —— <60 秒每千播放评论 **1.52** vs ≥60 秒 **11.90**。能买曝光，买不到关系 | `method-viral` 平台风向 |
| **★ 互动集中度** = Top 5 评论者 ÷ 总评论。实测 170 条评论来自 68 人，**12 人贡献 61%**。看起来有互动，实际是熟人圈回声 | `method-account` 漏斗诊断 |
| 漏斗四层写明观测量，**信任层从「评论数」改为「独立评论者数」** | `method-account` 漏斗诊断 |

⚠️ **样本边界**：单账号、单平台、n=218、无爆款。**能证伪，不能证明普适。**
所有 ✅ 只说明「在一个真实小账号上成立」。

## [0.9.0] - 2026-08-09

### 让两份核心方法论真正被用上

**问题**：`method-account.md` 与 `method-viral.md` 长期被跳过。
根因有两个，两头都以为对方覆盖了：

1. `SKILL.md` 的方法论索引写着「**需要时再读，不要预读**」—— 实测结果是**从不读**
2. `11-audit` 的检查清单只列了「闸门 + 爆款元素 + 四条件 + 四来源」，
   **漏了作品目的五型、脚本结构六种、爆款三类型**

后果不是少读两份文档，是**判据缺失而不自知**：
按清单查是合格的，打开方法论逐条对**缺三项**，
另有两项从没落实过（IP 五要素的**道具**、**漏斗形状从未画过**）。

### Changed

- **`SKILL.md` 方法论索引分级**：`method-viral` 与 `method-account` 标为
  **对应环节必读**（3-topic / 1-account / 4-hook / 7-review），
  其余按需。并写明「清单是方法论的摘要，不是替代品」
- **`workflows/3-topic.md` 新增「选题卡最小字段集」**：12 个字段的勾选清单，
  把两份方法论的必做项合并成一处。带 ★ 的六项标明是最容易漏的，
  其中三项给了硬判据（八标签一个没命中就重做 · ②群体够大是唯一真闸门 ·
  起号期优先涨粉型）
- **按线定不逐条标**：同一内容线里作品目的/爆款类型/脚本结构/呈现形式基本一致，
  在线上定一次即可；**走机动位的一次性弹药必须单独标**
- **`workflows/11-audit.md`**：
  - `3-topic` 检查行补全为完整字段集
  - 新增「★ 先对照方法论，再对照本表」一节
  - 「怎么查有没有真读」补一条：**说得出规则但说不出它在哪份文件里，同样是没读**

### Added · 两条失效模式

- **清单合格但方法论缺项** —— 症状是审计结论「合格」，
  但作品目的、脚本结构、爆款类型三项从来没标过
- **「只要你要」六选题硬凑** —— 那六个是给**创始人 IP** 的。
  用户不是创业者时多数不适用，硬凑就是编造经历，撞 `9-deliver` 第二条。
  **能对上一两个就写一两个**

## [0.8.0] — 2026-08-09

一次真实复盘带来的方法论修正。依据：43 个账号 2,130 条笔记全量拆解 +
约 900 条评论原文 + 官方规则原文。**每条结论都带证据写进了文件。**

### 被证伪并移除
- **系列名**：采用率 0.8%，与数据零相关（ρ=+0.05），同号配对 3 胜 3 负，
  唯一真做编号系列的号跑输自己 40%。机制原因：分发是单条的，
  「第 N 集」对没看过前 N−1 集的陌生人是负资产
- `3-topic` 五、优先做系列 → **优先做内容线**，连续性改由昵称 / 标题句式 /
  封面承担；系列命名四关保留但改为**条件句**，并推广到「一切写死的承诺」
- **计数机制**（无终点无参照）已证伪，与**进度条**（有终点）区分开
- **封面比例处方移除**：3:4 的优势按月分层后消失，是发布期混杂
- **时长处方移除**：跑赢样本跨度 20–194 秒，中位数是描述不是处方

### 新增机制
- **转化 = 闸门，不是流量**：转化只发生在「必须私信才能拿到的具体东西」存在时。
  决定性对照：表格放图里 461赞/50评/转化 0；不放图里 79赞/25评/9 条求表格。
  **流量与转化不但脱钩，而且负相关**
- **收藏分叉**：内容型追收藏成立；要转化的账号收藏是陷阱——
  合集换收藏，单点换评论
- **涨粉只有两条被验证的路径**：模板路（能连发 60 次的模板）与
  连载路（能追一年的真实进行中的事）。并列结构不涨粉
- **爆款四条件第 4 条的唯一自检**：这条发出去，评论区会吵什么？
  答不上来就别发（同人同器材，6,359 赞 vs 223 赞，差在有没有可吵的点）

### 优先级修正
- `5-script` 开头新增：**画面解释不了数据差异**。230 条评论关键词扫描，
  画面类命中 6 条且零负面。分镜用来保证能拍完，不要指望它拉高数据
- 唯一值得投入的画面事项是**解放双手**（智能眼镜 / 运动相机）

### 平台规则辟谣
- **「账号要垂直」不是平台规则**——官方文件里没有这个参数，
  罚的是内容违规不是跨领域。民间清单互相打架且有错别字
- 更新频率 / 断更 / 删稿惩罚同样查无实据
- 导流红线是真的，且**谐音与图片同样被识别**；站内私信官方明确认可
- 特殊行业资质门槛：境外执照拿不到官方留资组件，
  **转化必须 100% 设计在站内私信里，这是架构约束**

### 其他
- `3-topic` 新增：内容线三问、形态测试第二道、闸门粒度（路线+时段，
  不是「开车」这个动作）、第四来源命中 0 的处理、存量与流量之分
- `1-account` 新增顺序设计：feed 不露职业 / 主页露，
  以及身份自述帖在此时升级为唯一职业入口
- `7-review` 新增：区分「这一条差」和「这一类差」，按类型分组算中位数
- `11-audit` 新增四种失效模式：操作层被统计量改写、给碎片单独立块、
  凭空设计形式与命名、相关当因果；并新增「用户连续否决时先怀疑前提」
- `method-account` 补：真实需求的自然语言、昵称格式、简介不放联系方式

---

## [0.7.0] — 2026-08-08

强制触发与可验证执行。

问题：用户无法判断助手是否真的按流程做事，导致大量返工。
原来的「开工第一句」只要求报环节名，不要求报读了哪些文件——
结果是可以写 `[编导] 走 3-topic` 却根本没打开那个文件。

### 可验证性
- description 加入「编导」显式触发词，并覆盖全部环节名与「做视频/做内容」话题
- 开工第一句升级为 `[编导] 走 <环节> · 已读 <文件>`，**声明前必须先 view**，
  工具调用是唯一凭证，不允许凭记忆写文件名
- 明确「参与写过 skill 不等于读过」——每次开工重新读
- 新增原则十二：四个选题来源的落实状态必须逐一交代（✅/🟡/❌）

### 防返工
- `3-topic` 新增「涉及本人经历先核对 `profile.ip_analysis`」；
  顺序错了也算错——「先做 A 再做 B」和「A 和 B 一起做」是两个故事
- `9-deliver` 通检六项 → 八项：编导声明+view 记录、经历事实核对
- profile 新增 `ip_analysis` 块（职业路径 / 最骄傲 / 最挫折 / 缺点 / 不可推断项）。
  规格里引用了这个字段但模板里没有，补上，否则三处引用都是悬空的

### 新增环节
- `workflows/11-audit.md` 全量审计：逐环节合规检查、七种常见失效模式、
  「怎么查有没有真读」的证据标准

---

## [0.6.1] — 2026-08-08

让「有没有被用上」这件事可见、可控。

- **描述补英文触发词**：原本几乎全中文，用英文问
  （"give me video topics for my Xiaohongshu account"）大概率不触发
- **新增「开工第一句」**：进入后必须先报 `[编导] 走 <环节>，产出 <文件>`。
  skill 是否生效，用户从外面本来看不出来——泛泛建议和真走流程长得一样
- README 增加一节：显式点名 / 说具体 / 自动触发三档可靠度，
  以及在 CLAUDE.md / AGENTS.md 里加一句让它总是生效

---

## [0.6.0] — 2026-08-08

profile 分离数据层与价值观层。

### 为什么
原来的 profile 偏向「数据能证明什么」，丢掉了「用户想成为什么样的人」。
数据能验证「做什么有效」，验证不了「该怎么做人」——两层都要，不是替代关系。

### 分层
- 新增核心原则九：数据决定拍什么 / 什么形态 / 什么配比；
  本人决定用什么态度 / 什么边界 / 什么语气
- profile 顶部声明两层，逐块标注 `★ 数据层` / `★ 价值观层`
- **永远不因「这样数据会更好」修改价值观层**

### tone 重构
- `_internal_*` 前缀区分内部原则与可说例句
- 新增 `表达标准.forbidden_sentences` 与 `correct_form`
- 新增「说与不说的分界」，含通用检验：
  这句话如果是别人说你，是不是在夸你？是 → 你自己不能说

### 补回缺失的块
`surface_rule` / `identity_exposure` / `reuse_policy` / `archive` / `local_content`
（`business` / `tone` / `production` 原本就有，未重复添加）

### 流程
- `2-ip` 新增三节：原则与台词的分界、表达标准的正确形态、访谈必问两句
- `1c-backfill` 新增边界说明：逆向导入只能反推数据层，**价值观层必须补做 2-ip**
- `9-deliver` 通检从四项加到六项，新增 `forbidden_sentences` 与
  `_internal_*` 泄漏检查
- A6 gate_1 三项命中即否决；gate_2 不得以「数据会更好」为由改价值观层
- A2 禁止把 `_internal_*` 写进口播 / 标题 / 字幕

---

## [0.5.0] — 2026-08-08

移除抖音。

### 移除
- 抖音没有可用的接口，留在文档里只是给用户一个做不到的承诺
- 平台从三个收敛为两个：小红书 / YouTube
- `platform-playbook` 删掉抖音整节并重编号，速查表、跨平台封装示例、
  配额与诊断口径一并改为两平台
- `mcp-tools`、`1d-scan`、`3-topic`、`4-hook`、`7-review`、
  `profile` / `topic-card` 模板、README 中的抖音引用全部清掉
- SKILL.md 的触发词去掉抖音

保留的历史条目里仍会提到抖音，那是当时的事实，不改。

---

## [0.4.0] — 2026-08-08

选题有了四个来源，剪辑有了硬停。

### 选题来源
- `3-topic` 新增「选题的四个来源」：你是谁 / 观众要什么 /
  平台现在在推什么 / **你拍得出什么**
- 第四个来源接素材库：每个候选标 `[今天能做]` 或 `[需补拍]`
- 「最近热门」的能力边界写清楚了：**没有热榜接口**。
  YouTube 可查「近 N 天发布 + 按播放排序」（真实近期表现）；
  小红书只能看关键词当下排名；抖音无接口。
  小红书 `list_feeds` 是**按你的画像推的**，当趋势用会把自己的兴趣误当大盘

### 剪辑闸门（硬停）
- 粗剪完成后**必须停下等用户裁决**，这是流程里唯一的硬停
- 交接三件事：做了什么 / 哪几处不确定 / 什么没做
- 用户说「通过」才渲染；说「要改」就留在本环节，改完再停一次
- 升为第四种循环，写进 SKILL.md

### 字幕
- 不再默认让剪辑软件重新转写。取用顺序：
  `vo_segment` 原稿（零错字）→ 素材库已有转写（带词级时间码与纠错回流）
  → 重新转写（兜底）
- 旁白是自己写的稿，让机器再听一遍只会引入错字

---

## [0.3.2] — 2026-08-08

端到端实测，修正了三处只靠读源码得出的错误结论。

### 实测环境
- 素材库 1120 条，剪辑软件真实建库建轨，小红书 / YouTube 已验证在线

### 修正（这些原文档是错的）
- `record_frame` **是必填项**，原文档完全没提，少了直接报错
- `record_frame` **从 0 起算**（相对时间线起点）。空时间线 `start_frame`
  显示 108000，照抄会把内容推到一小时之后——实测两种填法长度相差正好 108000 帧
- `clip_id` 指**媒体池 unique_id**，与素材库 clip_id 不通用，中间要做映射
- 剪辑 MCP 默认只暴露 34 个聚合工具，文档里的工具名需 `--full`

### 新增实测结论
- `find_broll` 是**词面重合打分**，不是语义检索：用户标签 +4、OCR +3、
  AI 标签 +2、描述词 +1。**打标签比改措辞有用得多**
- **分数要设下限**：库里没有的题材仍返回 1.2–1.4 分；<2 视为没找到，≥4 才算命中
- `find_broll` **返回整条素材，不给 in/out**；实测单条 28 分钟，
  视觉描述只在 3 个锚点（25%/50%/75%）。**镜头级 in/out 仍需人工**
- 索引的 `fps`/`width`/`height` 全为 0，真实帧率须 `ffprobe` 探文件
- 素材 29.97 与时间线 30 帧率不一致，长片累积漂移
- 剪辑软件会被脚本接口自动拉起，不需先手动打开

### 仍未验证
- 声音克隆与配音生成（需用户授权且消耗额度）
- 从配音轨自动生成字幕（需先有配音）

---

## [0.3.1] — 2026-08-08

配音从「两种」修正为「三种」。

### 修正
- 上一版把「AI 配音」当成一件事，判断过严：
  **本人声音克隆**与通用合成音对账号的影响完全不同
- 新判断表按三种模式分列：真人现录 / 本人克隆 / 通用合成音
- 本人克隆 + b-roll 里有本人画面 → `self_presence` 正常通过，
  不因配音是克隆的而额外扣分；画面里没有本人才是问题
- 人设类 / 观点类对本人克隆放行（原为「不要」）；
  **态度类仍须自己录**——语气匹配是「永远保留人工的四项」之一

### 新增
- 混录：关键几句自己录 + 其余用克隆，记在 `vo.human_recorded_segments`
- 克隆音的技术限制：一般只支持 TTS，不支持用自己的声音重配自己的录音，
  因此念稿感无法靠「翻唱粗剪」绕过
- 压念稿感的五条做法：口语写稿、短句、显式断句、混录、语速对齐风格
- `profile.styles.<风格>.fixed_elements.voice`：克隆音建一次长期复用
- 合规：合成语音的标识要求写进 `profile.compliance`，按平台各自标注
- `edit-plan.vo` 结构改为 `mode` / `voice` / `human_recorded_segments`

---

## [0.3.0] — 2026-08-08

b-roll + 配音的端到端自动化。

### 新增
- `workflows/6b-autoedit.md`：产能最高形态的完整流水线——
  配音 → 按段找 b-roll → edit-plan → 搭时间线 → 字幕 → 通看 → 渲染
- 素材库（VideoSemantics）接入：`find_broll` 按旁白文本找候选，
  配合 `find_clips` / `search_clips` / `get_transcript` 三级降级
- 剪辑软件（DaVinci Resolve）接入：导入 → 建时间线 → 铺配音 →
  `timeline_create_subtitles_from_audio` 自动字幕 → 渲染
- TTS 接入：分段生成、固定音色、音色 ID 写进 profile 复用
- `edit-plan.json` 新增 `vo` 段（含 `synthetic` 标记）、
  每镜 `clip_id` / `source_path`、`autoedit` 段（时间线名与帧率）

### AI 配音的边界（重要）
- 合成音与两条既有规则冲突：`self_presence` 与「人味儿即可信度」
- 明确判断表：搜索型 / 攻略 / 榜单 / 氛围向可用；
  **人设类、态度类、商业转化类不用**
- 用了合成音仍须插入本人画面，或显式归为「无人设内容」并受配额约束
- 新增四条告警：合成音用错内容、无人设内容超配额、音色不一致、帧率不一致

### 已知坑
- `append_to_timeline` 只给片段 ID 会铺整段素材，
  in/out 须按**时间线帧率**换算成帧号传 `clip_infos`

---

## [0.2.0] — 2026-08-08

分平台 + 可读真实数据 + 流程不再僵化。

### 分平台
- 新增 `references/platform-playbook.md`：小红书 / 抖音 / YouTube 各自的
  评判标准、判成败指标、常见误判、跨平台封装取舍
- `3-topic` 新增「分平台封装」一节：内核跨平台，标题/形态/时长/判据分平台。
  「这条只适合抖音」是合格结论
- `7-review` 新增「先分平台，再下结论」：三平台都差=内容问题，
  只有一个差=适配问题。用错平台标准比没标准更糟
- `profile` 新增 `platforms` 段，`topic-card` 新增 `platform_fit` 段

### MCP
- 新增 `references/mcp-tools.md`：xiaohongshu-mcp 与 youtube-mcp-server 的
  工具清单、各环节怎么用、退化规则
- 新增 `workflows/1d-scan.md`：有工具时自动扫号，判断逻辑与 `1c-backfill` 一致，
  只是把「让用户贴」换成「自己读」
- 字幕反推语言习惯（不耗配额），评论区反推真实人设
- 抖音无可用 MCP，明确走人工，不假装有

### 生成优先（不再是「审稿器」）
- 新增原则八「默认生成，不默认评判」：闸门是筛选器不是入口，
  先生成一批再筛，而不是等用户拿东西来审
- 默认产出量：选题 8 个、标题 10 个、开头 5 句、封面 3 个、系列名 5 个
- `3-topic` 调整为「先生成，再筛」，闸门移到生成之后逐个筛
- `4-hook` 标题从 3 个提到 10 个，「这个标题怎么样」先给十个更好的
- 新增「我想拍一个 X 的 vlog」这类生成型请求的处理路径

### 长期记忆与自我改进
- 新增原则九「记住的东西不要再问第二遍」
- `profile.styles.<风格>.fixed_elements`：固定片头、口头禅、固定环节、
  BGM、转场、片尾——说过一次永久复用，`5-script` / `6-edit` 自动带上
- `profile.learned`：`works` / `fails` / `audience` / `open_questions`
- `7-review` 新增「写回 profile」：验证过的结论落盘，
  `3-topic` 与 `4-hook` 开工前先读，用得越久给的东西越准

### 流程
- 新增原则七「有什么用什么，缺什么补什么」：不要求填齐再开工，
  能推的标 `inferred`，只问缺了就做不下去的
- 新增原则八「人性不变，平台标准变」
- `0-intake` 重写为「先榨干已有的 → 再读 → 再推断 → 最后才问」
- 三种用户 → 四种用户，且四条入口可混用（小红书有号 + YouTube 零基础）
- 开工前四查 → 六查，加入平台与工具探测

## [0.1.0] — 2026-08-08

首个版本。

### 结构
- 网状路由：统一入口直达任何环节，支持横向跳转与三种循环
- 13 个环节：需求澄清 / 账号定位 / 素材挖掘 / 逆向导入 / 人设 IP / 选题 / 钩子 / 脚本分镜 / 剪辑交付 / 复盘诊断 / 风格档案 / 统一验收 / 编导训练
- 5 个结构化模板：profile · topic-card · script · edit-plan · ledger
- 3 份方法论参考：账号体系 / 爆款与起号 / 热点与语言习惯

### 发布层
- 新增 `automation/`：发布策略、配额（全比例制）、平台能力、ledger 完整结构、agent 职责
- 与创作层通过 `ledger` 连接；`7-review` 的漏斗诊断依赖本层
- 新增 `docs/sync-notes.md` 同步清单，两层共用字段全部标注 `# SYNC:`
- 新增维度：`style` 进入 ledger 与配额（v0.1 新增，待实测验证）

### 存档
- `archive/` 收录研究笔记（不打包进 skill，建议仓库保持 Private）

### 覆盖能力
- 三种用户入口：零基础 / 有账号有数据 / 中间态
- 三种剪辑交付：AI 全自动 / 人自己剪 / 半自动
- 多风格识别：风格作为一组剪辑参数，剪辑前先识别
- 三种循环：验收不过回原环节 / 复盘回上游 / 训练补短板
